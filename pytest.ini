[pytest]
pythonpath = .
addopts = --import-mode=importlib
markers =
    unit: fast isolated tests for game and core contracts
    integration: cross-component integration tests
    slow: longer-running tests (training/checkpoint IO)
