[project]
name = "alphazero-board-games"
version = "0.1.0"
description = "AlphaZero implementation for board games (Gomoku, and more)"
requires-python = ">=3.12"
dependencies = [
    "numpy>=1.24",
    "keras>=3.13.1",
    "tensorflow>=2.16",
]

[dependency-groups]
dev = [
    "dotdict>=0.1",
]
